import React from 'react';
import { Text, View } from 'react-native';

const Dashboard = ({ navigation }) => {
    return (
        <View
            style={{
                flex: 1,
                justifyContent: 'center',
                alignItems: 'center',
            }}>
            <Text>You made it gg!</Text>
        </View>
    );
};
export default Dashboard;